# Phoenix-Package_handler (archived 2026-09-13)

This is one of two archived standalone package-handler checkouts — see
`../README.md` (one level up) for the full story: why both were archived, what
was verified before archiving, and the worker-name safety fix applied to
both. Its sibling is `../package-handler/` — genuinely near-identical code,
each still its own git repo/remote.

Not kept in sync with its sibling's CONNECTIONS.md by design (a prior version
of this file kept flipping which repo it described every time a `git merge`
touched both — see git log if curious). This shared, minimal version replaces
that pattern rather than repeating it a fourth time.

The only active package-handler copy is `Phoenix-DevOps-oS/sector2/package-handler/`.
